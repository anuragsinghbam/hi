name: My first workflow
on: [push, workflow_dispatch]
env:
  EC2_HOST: 13.203.175.237
  EC2_USER: ubuntu
jobs:
  my-first-job:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
    - name: Create output
      id: hello
      run: echo "message=Hello from step" >> $GITHUB_OUTPUT
    - run: echo "${{toJson(steps)}}"
