name: First Workflow
on: workflow_dispatch
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: first-step
              run: |
                ls -a
                pwd
                cd /
                pwd
                ls -a
                cd
                ls -a
                echo "Node Version"
                node -v
                echo "Git Version"
                git -v
                echo "Docker Version"
                docker -v
